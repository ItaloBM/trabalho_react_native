// components/Lobby.js
import React from 'react';
import { View, Text, Button } from 'react-native';
import { Container } from '../styles/HomeStyles'; // Ajuste conforme necessário

const Lobby = () => {
  return (
    <Container>
      <Text>Tela de Lobby</Text>
      {/* Adicione mais conteúdo conforme necessário */}
    </Container>
  );
};

export default Lobby;